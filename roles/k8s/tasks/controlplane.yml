---
  - shell: kubeadm join {{LOAD_BALANCER_DNS}}:{{LOAD_BALANCER_PORT}} --cri-socket '/var/run/crio/crio.sock' --token "{{ lookup('file', role_path + '/files/TOKEN') }}" --discovery-token-ca-cert-hash sha256:"{{ lookup('file', role_path + '/files/HASH') }}" --control-plane --certificate-key "{{ lookup('file', role_path + '/files/CERTIFICATE') }}" --ignore-preflight-errors=NumCPU
